import { PriceFormationPipe } from './price-formation.pipe';

describe('PriceFormationPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceFormationPipe();
    expect(pipe).toBeTruthy();
  });
});
